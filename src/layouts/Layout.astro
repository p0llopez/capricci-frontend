---
import "@fontsource-variable/league-spartan"
import { ViewTransitions } from "astro:transitions"
import Header from "../components/Header/Header.astro"

interface Props {
	title: string
	onlyLogo?: boolean
}

const { title, onlyLogo } = Astro.props
---

<!doctype html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="Astro description" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
		<ViewTransitions />
	</head>
	<body class="color-bluegray overflow-scroll bg-beige text-bluegray">
		<Header onlyLogo={onlyLogo} />
		<main class="flex flex-col items-center justify-center px-2 py-4 sm:px-4 xl:px-40">
			<slot />
		</main>
		<style is:global>
			body {
				font-family: "League Spartan Variable", sans-serif;
			}
		</style>
	</body>
</html>
